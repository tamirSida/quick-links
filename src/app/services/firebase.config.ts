// Firebase configuration
// Replace these values with your Firebase project configuration
export const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "your-app-id"
};

// Instructions for setup:
// 1. Go to Firebase Console (https://console.firebase.google.com/)
// 2. Create a new project or select existing one
// 3. Add web app to your Firebase project
// 4. Copy the configuration object and replace the values above
// 5. Enable Authentication with your preferred providers
// 6. Create Firestore database in production mode
// 7. Set up Firestore security rules for user-based access